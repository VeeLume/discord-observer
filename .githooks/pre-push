#!/usr/bin/env bash
set -euo pipefail

BUNDLE="${PLUGIN_BUNDLE:-icu.veelume.starcitizen.sdPlugin}"

echo "▶ cargo fmt --check"
cargo fmt --all -- --check

echo "▶ cargo clippy"
cargo clippy --all-targets --all-features

echo "▶ cargo check"
cargo check --all-targets --all-features

echo "✓ pre-push checks passed"
